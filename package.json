{
  "name": "theoreum-attendance",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "node --check api/rpc.js && node --check api/kiosk-mark.js && node --check api/attendance-log.js && node --check api/absent-run-cron.js && node --check lib/absent-direct.js && node --check lib/attendance-notify-queue.js && node --check lib/staff-auth.js && node --check lib/staff-attendance.js && node --check lib/staff-qr-core.js && node --check lib/student-qr-core.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.101.1",
    "vite": "^5.0.0"
  }
}
